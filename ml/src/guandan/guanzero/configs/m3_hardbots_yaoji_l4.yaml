# Yaoji-focused continuation branched from guanzero_hardbots_25k @ 35k.
#
# 60% vs hard bot, 40% self-play (was 40/60 in m3_hardbots_l4). Hard-bot
# sampling weighted toward yaoji + jidan (the two opponents where DMC
# stalled around coin-flip): yaoji 40%, jidan 40%, strategic 20%.
#
# Sample-mix math: 4·0.40 self-play units + 2·0.60 vs-hard-bot units
# → 57% self-play / 43% vs-hard-bot in the buffer; of that 43%,
# yaoji and jidan each get ~17%, strategic ~9%.
#
# Launch:
#   modal run --detach ml/scripts/modal/train_guanzero_modal.py \
#     --config-path /root/ml/src/guandan/guanzero/configs/m3_hardbots_yaoji_l4.yaml \
#     --updates 45000 \
#     --run-name guanzero_hardbots_25k \
#     --resume /runs/guanzero/guanzero_hardbots_25k/checkpoints/update_00035000.pt \
#     --seed 0

n_actors: 32
sync_interval_updates: 500
sync_jitter_updates: 100
actor_push_batch_size: 512
sample_queue_maxsize: 64
max_drain_batches_per_loop: 16
publish_interval_updates: 200
checkpoint_every_updates: 5000
log_every_updates: 10
updates_per_learner_step: 1

model_type: shared_heads
shared_head_role_d_model: 128
shared_head_history_hidden: 256
shared_head_global_hidden: 128
shared_head_action_hidden: 128
shared_head_trunk_hidden: 1024
shared_head_trunk_layers: 4
dropout: 0.0

use_inference_server: false
inference_device: cuda
inference_batch_max_requests: 32
inference_batch_max_action_rows: 4096
inference_batch_timeout_ms: 5.0
inference_max_actions: 512
inference_n_slots: 512

target_replay_ratio: 1.0
max_replay_ratio: 1.25
max_throttle_sleep_s: 0.05

lr: 1.0e-4
gamma: 1.0
batch_size: 4096
buffer_capacity: 400000
buffer_capacity_per_player: 100000
buffer_min_size: 8192

# ── Epsilon (latest only; bots are deterministic Agent.act calls) ──
epsilon_start: 0.02
epsilon_final: 0.02
epsilon_decay_updates: 0
epsilon_frozen: 0.00
is_partner_visible: true

# ── Hard-bot mixed-opponent self-play, yaoji/jidan-focused ──
latest_vs_latest_frac: 0.40
latest_vs_hard_bot_frac: 0.60
hard_bot_pool:
  - strategic
  - yaoji
  - jidan
hard_bot_sampling:
  strategic: 0.20
  yaoji: 0.40
  jidan: 0.40

device: cuda
use_bf16_learner: true
compile_mode: reduce-overhead
